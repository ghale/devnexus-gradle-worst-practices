apply plugin: 'org.ajoberstar.github-pages'

githubPages {
    repoUri = 'git@github.com:ghale/G3-gradle-worst-practices.git'
    pages {
        from file("$buildDir/asciidoc/revealjs")
    }
}

publishGhPages.dependsOn(asciidoctor)
